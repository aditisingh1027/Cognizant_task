# Quick Sort Demo

This demo implements the quick sort algorithm, an efficient divide-and-conquer sorting method with average time complexity **O(n log n)**. The implementation sorts an integer array in place.

## How to run
`csharp
DSA.QuickSort.QuickSortDemo.Run();
`
